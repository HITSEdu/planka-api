meta:
  environment: docker
  toolchain:
    name: docker-compose
    version: latest

build:
  dockerfile: Dockerfile
  skip: false

run:
  image: null
  command: null
  args: null
  persistenceMount: /data
  containerPort: "8080"
  compose:
    enabled: true
    file: docker-compose.yml
    service: app